// consider we want to replicate YouTube's caching mechanism when playing a video
// we have a video that at a given moment shows a random number of pixels
// using closures, create a function called playVideo

// const playVideo = () => {
    
// };

// const MediaPlayer = playVideo();
// console.log(MediaPlayer(2));
// console.log(MediaPlayer(2));
// console.log(MediaPlayer(4));